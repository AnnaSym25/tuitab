# Начало работы

> 🇬🇧 [This page in English](../en/getting-started.md) · [← Оглавление документации](README.md)

## Установка

Самый быстрый способ — через Cargo:

```sh
cargo install tuitab
```

Устанавливаются три одинаковые команды — `tuitab` и более короткие псевдонимы
`ttab` и `ttb`. Остальные способы (Homebrew, AUR, Debian, готовые сборки)
описаны в [основном README](https://github.com/denisotree/tuitab/blob/master/README.md#installation).

## Открыть файл

Передайте поддерживаемый файл первым аргументом:

```sh
tuitab data.csv
tuitab report.parquet
tuitab books.xlsx
tuitab app.db
```

| Формат | Расширения | Примечания |
|--------|-----------|------------|
| CSV / TSV | `.csv`, `.tsv` | Разделитель определяется автоматически; задаётся через `-d` |
| JSON | `.json` | Открывается деревом над самим документом |
| JSONL / NDJSON | `.jsonl`, `.ndjson` | По записи на строку |
| YAML | `.yaml`, `.yml` | Дерево, как JSON |
| TOML | `.toml` | Дерево, как JSON |
| Parquet | `.parquet` | Колоночный, быстрый |
| Arrow / Feather | `.arrow`, `.feather`, `.ipc` | Читается и пишется |
| Excel | `.xlsx`, `.xls` | Книги с несколькими листами открываются списком листов |
| Markdown | `.md`, `.markdown` | Страница — одна строка: поля фронтматтера становятся столбцами, текст — `body`, путь — `file` |
| SQLite | `.sqlite`, `.sqlite3`, `.db` | Таблицы открываются списком таблиц |
| DuckDB | `.duckdb`, `.ddb`, `.db` | Какой это движок, решает заголовок самого файла, а не его имя |

Для книг и баз данных tuitab сначала показывает обзор (листы или таблицы);
нажмите `Enter` на строке, чтобы открыть её, и `Esc` / `q`, чтобы вернуться.
Таблицу можно править и сохранить обратно — см. [Базы данных](database.md).

JSON, YAML и TOML не разворачиваются в плоскую копию: лист — это вид на сам
документ, `Enter` уходит внутрь вложенного объекта или списка, а сохранение
сериализует дерево обратно. Конвертация между ними — это просто другое
расширение при `Ctrl+S`.

![TOML-файл, отредактированный на месте и сохранённый как YAML](https://raw.githubusercontent.com/denisotree/tuitab/master/.github/assets/tree.gif)

### Задать формат принудительно

`-t` обязателен для stdin, а для файла он перекрывает расширение:

```sh
tuitab -t yaml deploy.conf     # YAML-файл, который называется не .yaml
```

Неизвестное расширение определяется по содержимому, так что `deploy.conf`
обычно открывается верно и без флага.

### Открыть базу, которой ещё нет

```sh
tuitab inventory.sqlite        # файла нет — открывается пустой лист
```

Добавьте столбцы, задайте им типы, добавьте строки, `Ctrl+S` — и tuitab запишет
настоящую типизированную таблицу. См. [Базы данных](database.md).

### Задать разделитель

Когда автоопределение ошибается (или файл разделён `;`):

```sh
tuitab data.csv -d ';'
```

## Просмотр нескольких файлов

Передайте больше одного пути — и tuitab откроет **список файлов** вместо одной
таблицы:

```sh
tuitab orders.csv customers.csv products.parquet
```

Каждый файл становится строкой с именем, размером и временем изменения. `Enter`
открывает выделенный файл; `Esc` / `q` возвращает к списку.

## Много файлов как одна таблица

Шаблон в кавычках достаётся tuitab, а не шеллу, и все файлы, которые он назвал,
читаются как одна таблица:

```sh
tuitab 'data/*.csv'
tuitab 'content/**/index.md'
```

Кавычки — и есть вся разница. Без них шелл раскроет шаблон в несколько аргументов,
и получится список файлов выше: он хорош, чтобы выбрать один файл, а не чтобы
прочитать их вместе.

Табличные файлы обязаны сойтись колонками; тот, который не сошёлся, называется в
ошибке, а не подмешивается молча — таблица, склеенная из несовпадающих кусков, это
не ответ, а каша со счётчиком строк. Страницы markdown — записи, поэтому шаблон по
ним объединяет: поле, которого на странице нет, приходит NULL. Это и превращает
статический сайт в таблицу, которую можно группировать и считать.

Шаблон — не файл. Перезагружать листу неоткуда, а `Ctrl+S` спросит имя, вместо того
чтобы предложить путь со звёздочкой.

## Просмотр каталога

Укажите tuitab папку (или запустите без аргументов, чтобы взять текущую):

```sh
tuitab ./reports/
tuitab               # текущий каталог
```

Откроется навигатор по файлам — открывайте файлы по `Enter`, выходите назад по
`Esc` / `q`.

## Чтение из конвейера

tuitab читает stdin, когда данные передаются по конвейеру. Укажите формат через
`-t`:

```sh
cat data.csv | tuitab -t csv
psql -c "SELECT * FROM orders" --csv | tuitab -t csv
echo '[{"id":1,"name":"Alice"}]'   | tuitab -t json
```

Из stdin понимаются `csv`, `tsv`, `txt`, `json`, `jsonl`/`ndjson`, `yaml`/`yml`
и `toml` (`txt` обрабатывается как CSV). Чтобы прочитать Parquet, Arrow, Excel,
Markdown, SQLite или DuckDB, открывайте файл по пути, а не через конвейер. `-`
работает как явный путь-stdin: `cat data.csv | tuitab -t csv -`.

## Экран

```text
┌ sample.csv ───────────────────────────────────────┐
│  id  s│ name        s│ age #│ salary  ~│ department s│   ← строка заголовка (имя + значок типа)
│▌ 1    │ Alice Johnson│ 30   │ 75000.00 │ Engineering │   ← строка под курсором (подсвечена)
│  2    │ Bob Smith    │ 45   │ 92000.50 │ Management  │
│  …                                                  │
├─────────────────────────────────────────────────────┤
│ NORMAL   Loaded 20 rows                  row 3/20 col 3/5 │  ← строка состояния
└─────────────────────────────────────────────────────┘
```

- В **заголовке** для каждого столбца показано имя и небольшой **значок типа**
  (`s` строка, `#` целое, `~` дробное, `d` дата, `t` дата-время,
  `%` проценты, `$` валюта, `B` размер файла, `?` логический).
- В **строке состояния** виден текущий режим, сообщение и позиция.
- В любой момент нажмите `?`, чтобы открыть встроенную справку.

## Дальше

- Изучите [горячие клавиши](keybindings.md).
- Попробуйте [графики](charts.md), [сводные таблицы](pivot.md) и [JOIN](join.md).
- Отредактируйте таблицу и запишите её обратно — [Базы данных](database.md).
- Дайте ассистенту считать по вашим файлам через [MCP-сервер](mcp.md)
  (`tuitab --mcp`).
- Загляните в практические [рецепты](recipes.md).
